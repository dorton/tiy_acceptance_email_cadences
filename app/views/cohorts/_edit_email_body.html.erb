<%= simple_form_for(@cohort, remote: true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email_body, :autofocus=>true %>

  </div>

  <div class="form-actions">
    <%= f.button :submit, data: { "disable-with": "Saving..."  } %>
  </div>
<% end %>
